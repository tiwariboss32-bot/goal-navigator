

## Plan

### 1. Update Hero Buttons (`src/components/landing/HeroSection.tsx`)
- Remove the "Try Demo" button
- Add an "Early Access — Try Now for Free" badge/button with a distinct style (e.g., a subtle outlined button or inline badge with a sparkle icon)

### 2. Update Dashboard Mockup Background (`src/components/landing/HeroSection.tsx`)
- Change the mockup card's inner `bg-card` to include a visible grid pattern overlay (using CSS background-image with subtle grid lines) instead of a plain dark background, making it feel more technical/modern

